source 'https://github.com/CocoaPods/Specs.git'

use_frameworks!
platform :ios, '13.0'


# ignore all warnings from all pods
inhibit_all_warnings!

target 'ArchitecturePrototype' do

  # Backend helpers
  pod 'Sourcery'
  pod 'SwiftLint'
  pod 'SwiftGen'
  
  target 'ArchitecturePrototypeTests' do
    inherit! :search_paths
  end
end
